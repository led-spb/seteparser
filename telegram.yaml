type: telegram
once: true
token: !secret telegram_token
chat_id: !secret telegram_chat_id
template: |
  <b>{{item.category}}</b>
  <a href="{{item.src}}">{{item.title}}</a>
  {{item.body}}
download_video: True
only_video: True
ydl_opts:
   format: "HTTP_CLOUD_MOBILE-552"
